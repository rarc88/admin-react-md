import React from 'react';
import IconButton from '@material-ui/core/IconButton';

export const MyIconButton = ({ children }) => {
  return (
    <IconButton>
        { children }
    </IconButton>
  )
}